# Technology Stack

**Analysis Date:** 2026-03-30

## Languages

**Primary:**
- Kotlin 2.2.10 - All application source code (`app/src/main/java/`, `app/src/test/`, `app/src/androidTest/`)

**Secondary:**
- XML - Android resource files (`app/src/main/res/`, `app/src/main/AndroidManifest.xml`)

## Runtime

**Environment:**
- Android (JVM) — min SDK 28 (Android 9 Pie), target SDK 36

**JVM Toolchain:**
- Java 21 (toolchain, configured via `gradle/gradle-daemon-jvm.properties`)
- Source/target compatibility: Java 11 (`app/build.gradle.kts`)

**Package Manager:**
- Gradle 9.3.1 (via wrapper at `gradle/wrapper/gradle-wrapper.properties`)
- Lockfile: `gradle/wrapper/gradle-wrapper.properties` (SHA-256 verified)
- Version catalog: `gradle/libs.versions.toml`
- Kotlin code style: `official` (`gradle.properties`)

## Frameworks

**Core:**
- Jetpack Compose BOM 2024.09.00 - Declarative UI framework (resolves all `androidx.compose.*` versions)
- Material Design 3 (`androidx.compose.material3`) - Design system and component library

**UI Layer:**
- `androidx.activity:activity-compose` 1.13.0 - Compose integration for `ComponentActivity`
- `androidx.compose.ui:ui` (via BOM) - Core Compose UI primitives
- `androidx.compose.ui:ui-graphics` (via BOM) - Canvas, drawing, brushes
- `androidx.compose.ui:ui-tooling-preview` (via BOM) - `@Preview` support in Android Studio
- `androidx.compose.ui:ui-text-google-fonts` 1.10.6 - Google Fonts provider integration (declared but Manrope is bundled as local fonts in `app/src/main/res/font/`)

**Lifecycle:**
- `androidx.lifecycle:lifecycle-runtime-ktx` 2.10.0 - Lifecycle-aware coroutine scopes and extensions

**Testing:**
- JUnit 4.13.2 - Unit test runner (`app/src/test/`)
- `androidx.test.ext:junit` 1.3.0 - AndroidX JUnit extensions for instrumented tests
- `androidx.test.espresso:espresso-core` 3.7.0 - UI interaction testing for instrumented tests
- `androidx.compose.ui:ui-test-junit4` (via BOM) - Compose testing utilities
- `androidx.compose.ui:ui-test-manifest` (via BOM, debugImplementation) - Test manifest for Compose

**Build/Dev:**
- Android Gradle Plugin (AGP) 9.1.0 - `com.android.application` plugin
- `org.jetbrains.kotlin.plugin.compose` 2.2.10 - Kotlin Compose compiler plugin
- `org.gradle.toolchains.foojay-resolver-convention` 1.0.0 - JVM toolchain auto-provisioning (`settings.gradle.kts`)

## Key Dependencies

**Critical:**
- `androidx.compose:compose-bom:2024.09.00` - Pins all Compose library versions; upgrade this to update the entire Compose suite
- `androidx.compose.material3:material3` (via BOM) - All UI components; the entire design system is built on M3 tokens

**Infrastructure:**
- `androidx.core:core-ktx` 1.18.0 - Kotlin extensions for core Android APIs

## Configuration

**Environment:**
- `local.properties` - SDK path (generated by Android Studio/Gradle, not committed)
- No `.env` files or runtime secrets detected

**Build:**
- Root: `build.gradle.kts` (top-level plugin declarations, apply false)
- App module: `app/build.gradle.kts` (full Android + Compose config)
- Settings: `settings.gradle.kts` (repo management, single `:app` module)
- Version catalog: `gradle/libs.versions.toml` (single source of truth for all versions)
- ProGuard: `app/proguard-rules.pro` (minification disabled for release builds currently)
- JVM args: `-Xmx2048m` (`gradle.properties`)

## Platform Requirements

**Development:**
- JDK 21 (auto-provisioned via Foojay toolchain resolver)
- Android SDK with API 36 compile SDK installed
- Android Studio recommended (workspace config in `.idea/`)

**Production:**
- Android 9 (API 28) and above
- Target: Android 16 (API 36)
- Application ID: `com.jna.tictactoe`
- Version: 1.0 (versionCode 1)
- Minification: disabled (`isMinifyEnabled = false` in release build type)
- Edge-to-edge display enabled (`enableEdgeToEdge()` in `MainActivity.kt`)

---

*Stack analysis: 2026-03-30*
