---
phase: 05-polish
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt]
autonomous: true
requirements: [5.1]

must_haves:
  truths:
    - "Winning triggers a line animation sweeping across the board"
    - "Pieces appear with a satisfying scale and fade effect"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt"
      provides: "Animated win line overlay and piece transitions"
  key_links:
    - from: "app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt"
      to: "Compose Animation APIs"
      via: "animateFloatAsState, AnimatedVisibility"
---

<objective>
Enhance the game board with professional animations for piece placement and winning.

Purpose: Provide visual delight and clear feedback on win scenarios.
Output: Refined piece animations and a dynamic win line drawing effect.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Animated Win Line Overlay</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt</files>
  <action>
    - Create a new Composable `WinLineOverlay(winLine: List<Int>?, boardSize: Float)` in `GameScreen.kt`.
    - The overlay should be a `Canvas` that sits on top of the `GameBoard`.
    - Use `animateFloatAsState` (from 0f to 1f) when `winLine` is not null.
    - Map the indices in `winLine` (e.g., [0,1,2]) to coordinate points on the board.
    - Draw a path that sweeps from the start to the end of the win line based on the animation progress.
    - Style the line with a primary or secondary gradient (matching the winner) and a rounded stroke.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>A line animates across the board when a player wins.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Piece Placement Animation</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt</files>
  <action>
    - Refine the `AnimatedVisibility` for `GameCell` to make it more "premium".
    - Use a custom `overshoot` interpolator or `spring` physics for the `scaleIn` enter transition.
    - Ensure the animation duration feels snappy (approx. 250-350ms).
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Piece placement feels punchy and satisfying.</done>
</task>

</tasks>

<verification>
Verify win line animation by winning a game. Check piece placement animation in any mode. Ensure app builds after animation changes.
</verification>

<success_criteria>
- Win line draws progressively over the winning cells.
- Piece placement includes a subtle bounce/overshoot scaling.
</success_criteria>

<output>
After completion, create `.planning/phases/05/05-02-SUMMARY.md`
</output>
