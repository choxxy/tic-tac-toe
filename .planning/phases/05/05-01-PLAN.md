---
phase: 05-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/src/main/java/com/jna/tictactoe/audio/SoundManager.kt, app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt, app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt]
autonomous: true
requirements: [POLISH-02]

must_haves:
  truths:
    - "User hears a sound when placing a piece"
    - "User hears a distinct sound when winning or drawing"
    - "User feels a subtle vibration on piece placement"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/audio/SoundManager.kt"
      provides: "SoundPool-based audio management"
    - path: "app/src/main/res/raw/place_piece.wav"
      provides: "Audio asset for placement"
  key_links:
    - from: "GameViewModel.kt"
      to: "SoundManager.kt"
      via: "Method calls in updateStateAfterMove and onCellClicked"
---

<objective>
Implement low-latency audio feedback and haptic sensations for game interactions.

Purpose: Provide multi-sensory feedback to make the game feel tactile and premium.
Output: A functional SoundManager and integration with the Game screen.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SoundManager and Audio Assets</name>
  <files>app/src/main/java/com/jna/tictactoe/audio/SoundManager.kt, app/src/main/res/raw/place_piece.wav, app/src/main/res/raw/game_win.wav, app/src/main/res/raw/game_draw.wav</files>
  <action>
    - Create `SoundManager.kt` in `com.jna.tictactoe.audio`.
    - Use `SoundPool.Builder` to initialize a `SoundPool` with `USAGE_GAME` and `CONTENT_TYPE_SONIFICATION`.
    - Implement methods to load and play sounds.
    - Create placeholder 1-second silent or simple beep WAV files in `app/src/main/res/raw/` to prevent build failures (actual assets can be replaced later).
    - Ensure `release()` method is provided to clean up resources.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>SoundManager is implemented and assets exist in res/raw.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Audio and Haptics into Game Screen</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt, app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt</files>
  <action>
    - Update `GameViewModel` to hold an instance of `SoundManager` (or receive it via constructor).
    - Call `soundManager.play(placePieceId)` in `onCellClicked` when a move is successfully applied.
    - Call `soundManager.play(winId)` or `soundManager.play(drawId)` in `updateStateAfterMove` when the game phase changes from `PLAYING`.
    - Ensure `soundManager.release()` is called in `onCleared()`.
    - In `GameScreen.kt`, use `LocalHapticFeedback.current` to trigger `HapticFeedbackType.LongPress` (or similar) inside the `onClick` handler of `GameCell` when a move is valid.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Audio and haptics are triggered during gameplay.</done>
</task>

</tasks>

<verification>
Check for SoundManager class existence and integration in GameViewModel. Verify haptic calls in GameScreen.
</verification>

<success_criteria>
- SoundManager uses SoundPool for low-latency playback.
- GameViewModel triggers sounds on move, win, and draw.
- GameScreen triggers haptic feedback on cell click.
</success_criteria>

<output>
After completion, create `.planning/phases/05/05-01-SUMMARY.md`
</output>
