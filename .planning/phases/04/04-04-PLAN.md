---
phase: 04-lan-multiplayer
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - app/src/main/java/com/example/tictactoe/game/GameViewModel.kt
  - app/src/main/java/com/example/tictactoe/ui/component/ReconnectingOverlay.kt
  - app/src/main/java/com/example/tictactoe/game/GameUiState.kt
autonomous: false
requirements: ["4.3"]
must_haves:
  truths:
    - "Disconnect is detected within 5 seconds"
    - "Reconnecting overlay appears automatically"
    - "Game resumes if connection restored within 9s"
  artifacts:
    - path: "app/src/main/java/com/example/tictactoe/ui/component/ReconnectingOverlay.kt"
      provides: "UI for connection recovery"
  key_links:
    - from: "app/src/main/java/com/example/tictactoe/game/GameViewModel.kt"
      to: "app/src/main/java/com/example/tictactoe/ui/component/ReconnectingOverlay.kt"
      via: "isReconnecting state property"
---

<objective>
Handle network interruptions gracefully through connection monitoring, automatic reconnection attempts, and a UI-based recovery process.

Purpose: Prevent abrupt game ends due to transient network failures.
Output: Disconnect detection logic and ReconnectingOverlay UI.
</objective>


<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/FEATURES.md
@app/src/main/java/com/example/tictactoe/game/GameViewModel.kt
@app/src/main/java/com/example/tictactoe/network/socket/GameSocketManager.kt

<interfaces>
<!-- From 04-01 -->
sealed class GameMessage {
    data object Heartbeat : GameMessage()
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Disconnect Detection</name>
  <files>
    app/src/main/java/com/example/tictactoe/game/GameViewModel.kt,
    app/src/main/java/com/example/tictactoe/game/GameUiState.kt
  </files>
  <action>
    - Add `isReconnecting: Boolean` and `reconnectCountdown: Int?` to `GameUiState`.
    - In `GameViewModel`, launch a heartbeat collector:
      - Send `GameMessage.Heartbeat` every 2 seconds.
      - If no messages (heartbeat or otherwise) are received for 5 seconds, or if the socket throws an `IOException`:
        - Set `isReconnecting = true`.
        - Start a 9-second countdown.
  </action>
  <verify>
    <automated>./gradlew test --tests "*GameDisconnectDetectionTest*"</automated>
  </verify>
  <done>GameViewModel detects connection loss and initiates recovery state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Recovery UI and Logic</name>
  <files>
    app/src/main/java/com/example/tictactoe/ui/component/ReconnectingOverlay.kt,
    app/src/main/java/com/example/tictactoe/game/GameScreen.kt
  </files>
  <action>
    - Create `ReconnectingOverlay`: A full-screen translucent overlay with a `CircularProgressIndicator` and "Reconnecting... [N]s".
    - In `GameViewModel`, if `isReconnecting` is true:
      - Periodically try to re-establish the socket connection.
      - If re-connected within 9 seconds: Reset `isReconnecting = false`.
      - If countdown hits 0:
        - Provide an option to "Play vs CPU" (switch to Local mode with Easy AI) or "Exit to Menu".
    - Integrate `ReconnectingOverlay` in `GameScreen`.
  </action>
  <verify>
    <automated>./gradlew test --tests "*GameRecoveryLogicTest*"</automated>
  </verify>
  <done>UI surfaces the reconnection process and provides fallback options after a timeout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify LAN Multiplayer and Recovery</name>
  <what-built>Full LAN Multiplayer functionality with recovery.</what-built>
  <how-to-verify>
    1. Start a LAN game between two devices (or emulators).
    2. Play a few moves to ensure sync works.
    3. Put one device into Airplane Mode or kill the app process.
    4. Verify the other device shows the "Reconnecting..." overlay.
    5. Re-enable network or restart app/reconnect within 9 seconds.
    6. Verify game resumes correctly.
    7. Test the 9-second timeout and fallback options.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Reconnection logic unit tests verify state transitions.
- Manual verification of the full game cycle under network failure.
</verification>

<success_criteria>
- Reconnection UI is non-blocking but prevents moves until sync is restored.
- The 9-second window is strictly enforced.
- Game can be saved by switching to CPU mode if the opponent doesn't return.
</success_criteria>

<output>
After completion, create `.planning/phases/04/04-04-SUMMARY.md`
</output>
