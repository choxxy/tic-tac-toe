---
phase: 04-lan-multiplayer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/example/tictactoe/network/model/GameMessage.kt
  - app/src/main/java/com/example/tictactoe/network/discovery/NsdDiscoveryManager.kt
  - app/src/main/java/com/example/tictactoe/network/socket/GameSocketManager.kt
autonomous: true
requirements: ["4.1"]
must_haves:
  truths:
    - "Can discover services on local network"
    - "Can establish TCP connection between two devices"
    - "JSON messages are serialized and deserialized correctly"
  artifacts:
    - path: "app/src/main/java/com/example/tictactoe/network/model/GameMessage.kt"
      provides: "Network message protocol"
    - path: "app/src/main/java/com/example/tictactoe/network/discovery/NsdDiscoveryManager.kt"
      provides: "Service discovery via NSD"
    - path: "app/src/main/java/com/example/tictactoe/network/socket/GameSocketManager.kt"
      provides: "TCP socket communication"
  key_links:
    - from: "app/src/main/java/com/example/tictactoe/network/socket/GameSocketManager.kt"
      to: "app/src/main/java/com/example/tictactoe/network/model/GameMessage.kt"
      via: "Kotlin Serialization"
---

<objective>
Establish the networking foundation for peer-to-peer communication using Android Network Service Discovery (NSD) and TCP Sockets.

Purpose: Enable devices to find each other on a local network and exchange structured game data without manual IP entry.
Output: Coroutine-friendly managers for discovery and socket communication, plus a serialized message protocol.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md

<interfaces>
<!-- Expected Message Types -->
@Serializable
sealed class GameMessage {
    @Serializable data class Handshake(val playerName: String) : GameMessage()
    @Serializable data class Move(val index: Int) : GameMessage()
    @Serializable data class SyncState(val gameState: GameState) : GameMessage()
    @Serializable data object Heartbeat : GameMessage()
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define GameMessage protocol</name>
  <files>app/src/main/java/com/example/tictactoe/network/model/GameMessage.kt</files>
  <behavior>
    - Test: Handshake message serializes/deserializes to JSON correctly.
    - Test: SyncState message serializes/deserializes with complex GameState.
    - Test: Move message serializes/deserializes correctly.
  </behavior>
  <action>
    Create a sealed class `GameMessage` annotated with `@Serializable`.
    Include subclasses for:
    - `Handshake(playerName: String)`
    - `Move(index: Int)`
    - `SyncState(state: GameState)` (reuse GameState from Phase 2)
    - `Heartbeat` (for connection monitoring)
    Use Kotlin Serialization (Json) for all operations.
  </action>
  <verify>
    <automated>./gradlew test --tests "*GameMessageTest*"</automated>
  </verify>
  <done>GameMessage class defined and verified with unit tests for JSON serialization.</done>
</task>

<task type="auto">
  <name>Task 2: Implement NsdDiscoveryManager</name>
  <files>app/src/main/java/com/example/tictactoe/network/discovery/NsdDiscoveryManager.kt</files>
  <action>
    Implement `NsdDiscoveryManager` wrapping `NsdManager`.
    - Provide `registerService(name: String, port: Int): Flow<NsdRegistrationEvent>`
    - Provide `discoverServices(): Flow<List<NsdServiceInfo>>`
    - Use `callbackFlow` to bridge mDNS callbacks to Coroutines.
    - Handle `NsdManager.DiscoveryListener` and `NsdManager.RegistrationListener`.
    - Use `ResolveListener` to resolve IP/Port before emitting found services.
    - Ensure listeners are cleaned up in `awaitClose`.
  </action>
  <verify>
    <automated>./gradlew test --tests "*NsdDiscoveryManagerTest*"</automated>
  </verify>
  <done>NsdDiscoveryManager can register and discover services on the local network.</done>
</task>

<task type="auto">
  <name>Task 3: Implement GameSocketManager</name>
  <files>app/src/main/java/com/example/tictactoe/network/socket/GameSocketManager.kt</files>
  <action>
    Implement `GameSocketManager` for bidirectional TCP communication.
    - Host mode: Create `ServerSocket(0)` (auto-port), accept one connection.
    - Client mode: Create `Socket(host, port)`.
    - Provide `send(message: GameMessage)` using `Json.encodeToString` + newline.
    - Provide `incomingMessages: Flow<GameMessage>` using `BufferedReader.readLine()` + `Json.decodeFromString`.
    - Run I/O operations on `Dispatchers.IO`.
    - Ensure socket/streams are closed on cancellation.
  </action>
  <verify>
    <automated>./gradlew test --tests "*GameSocketManagerTest*"</automated>
  </verify>
  <done>GameSocketManager enables reliable newline-delimited JSON communication between two sockets.</done>
</task>

</tasks>

<verification>
- Unit tests pass for all network components.
- Manual check: (If 2 devices available) Verify one device can see the other's mDNS advertisement.
</verification>

<success_criteria>
- Discovery logic is non-blocking and uses Coroutine Flows.
- Socket communication is type-safe via GameMessage and Kotlin Serialization.
- Infrastructure handles port assignment and resolution automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/04/04-01-SUMMARY.md`
</output>
