---
phase: 04-lan-multiplayer
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/src/main/java/com/example/tictactoe/ui/screen/lobby/LanLobbyScreen.kt
  - app/src/main/java/com/example/tictactoe/ui/screen/lobby/LanLobbyViewModel.kt
  - app/src/main/java/com/example/tictactoe/ui/navigation/NavGraph.kt
autonomous: true
requirements: [LAN-DISCOVERY, LAN-HOST-GUEST]
---

<objective>
Build the LAN Lobby UI to allow players to host games, discover peers, and exchange player information (handshake) before starting a match.

Purpose: Provide a user-friendly interface for connecting two players over the local network.
Output: LanLobbyScreen UI and LanLobbyViewModel handling peer connection and name exchange.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/FEATURES.md
@designs/wi_fi_lan_lobby/screen.png
@app/src/main/java/com/example/tictactoe/network/discovery/NsdDiscoveryManager.kt
@app/src/main/java/com/example/tictactoe/network/socket/GameSocketManager.kt

<interfaces>
<!-- From 04-01 -->
sealed class GameMessage {
    data class Handshake(val playerName: String) : GameMessage()
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LanLobbyScreen and ViewModel</name>
  <files>
    app/src/main/java/com/example/tictactoe/ui/screen/lobby/LanLobbyScreen.kt,
    app/src/main/java/com/example/tictactoe/ui/screen/lobby/LanLobbyViewModel.kt
  </files>
  <action>
    - Create `LanLobbyViewModel`: Manage state for "Host vs Join", "Discovery List", "Player Name", and "Connection Status".
    - Integrate `NsdDiscoveryManager` for service advertising/discovery.
    - Create `LanLobbyScreen` (Material 3 + Compose):
      - Tab switch for "Host Game" / "Join Game".
      - "Join Game" shows a list of discovered hosts using `LazyColumn`.
      - "Host Game" shows a "Waiting for opponent..." spinner with a "Stop Hosting" button.
      - "Player Name" text field at the top.
      - Follow `designs/wi_fi_lan_lobby/screen.png`.
  </action>
  <verify>
    <automated>./gradlew test --tests "*LanLobbyViewModelTest*"</automated>
  </verify>
  <done>LanLobbyScreen provides a UI for peer selection and hosting.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Handshake and Navigation</name>
  <files>
    app/src/main/java/com/example/tictactoe/ui/screen/lobby/LanLobbyViewModel.kt,
    app/src/main/java/com/example/tictactoe/ui/navigation/NavGraph.kt
  </files>
  <action>
    - In `LanLobbyViewModel`:
      - When a connection is established via `GameSocketManager`:
        - Send `GameMessage.Handshake(playerName)` to the peer.
        - Receive the peer's `GameMessage.Handshake`.
        - Store peer's name in state.
      - Upon successful handshake exchange, trigger a navigation callback to `GameScreen` with `LAN` mode and peer info.
    - Update `NavGraph.kt` to include the `LanLobbyScreen` route and handle navigation from `MainMenu` and to `GameScreen`.
  </action>
  <verify>
    <automated>./gradlew test --tests "*LanLobbyHandshakeTest*"</automated>
  </verify>
  <done>Connection handshake exchanges player names and navigates to the game.</done>
</task>

</tasks>

<verification>
- Verify `LanLobbyScreen` renders according to design.
- Mock `GameSocketManager` in unit tests to verify handshake logic.
- Verify navigation triggers only after peer name is received.
</verification>

<success_criteria>
- Discovery list populates automatically when "Join Game" is active.
- Hosting starts service advertisement and waits for a connection.
- Handshake ensures both sides have each other's name before the game starts.
</success_criteria>

<output>
After completion, create `.planning/phases/04/04-02-SUMMARY.md`
</output>
