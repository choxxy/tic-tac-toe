---
phase: 02
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt"
  - "app/src/test/java/com/jna/tictactoe/game/CpuPlayerTest.kt"
autonomous: true
requirements: ["AI-01", "AI-02", "AI-03"]
must_haves:
  truths:
    - "Hard AI never loses (optimal play)"
    - "Medium AI blocks immediate human wins"
    - "Easy AI makes valid random moves"
    - "AI logic is pure Kotlin (no android.* imports)"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt"
      provides: "AI move calculation logic"
    - path: "app/src/test/java/com/jna/tictactoe/game/CpuPlayerTest.kt"
      provides: "AI competency and optimal play tests"
  key_links:
    - from: "app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt"
      to: "app/src/main/java/com/jna/tictactoe/game/GameEngine.kt"
      via: "simulation of moves for minimax"
---

<objective>
Implement three levels of CPU AI (Easy, Medium, Hard) using heuristic and minimax strategies.
Purpose: Provide varying levels of difficulty for the VS CPU game mode.
Output: Functioning AI that ranges from random moves to unbeatable optimal play.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02/02-RESEARCH.md
@.planning/phases/02/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Easy and Medium CPU Strategies</name>
  <files>app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt</files>
  <action>
    Implement CpuPlayer as a Kotlin object with a main getMove(state, difficulty) function.
    - Easy: board.indices.filter { board[it] == EMPTY }.random()
    - Medium:
        1. Check if AI can win in one move -> Take it.
        2. Else check if Human can win in one move -> Block it.
        3. Else pick random empty cell.
    - Use pure Kotlin (no android.* imports).
  </action>
  <verify>
    <automated>./gradlew test -Ptest.filter=*CpuPlayerTest*</automated>
  </verify>
  <done>Easy and Medium strategies implemented and verified via unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Hard CPU Strategy (Minimax)</name>
  <files>app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt</files>
  <action>
    Implement the Hard strategy using the Minimax algorithm.
    - Recursive minimax implementation to search the game tree.
    - Scoring: Win (+10), Loss (-10), Draw (0).
    - Depth-Based adjustment: Subtract depth from win score (10 - depth) to favor faster wins.
    - Optimized for 3x3 board (performance < 100ms on modern JVM).
  </action>
  <verify>
    <automated>./gradlew test -Ptest.filter=*CpuPlayerTest*</automated>
  </verify>
  <done>Hard strategy implemented and verified as unbeatable.</done>
</task>

<task type="auto">
  <name>Task 3: Unit Tests for AI Logic</name>
  <files>app/src/test/java/com/jna/tictactoe/game/CpuPlayerTest.kt</files>
  <action>
    Create comprehensive tests for CpuPlayer:
    - Verify Medium AI blocks immediate wins.
    - Verify Medium AI takes immediate wins.
    - Verify Hard AI (Minimax) results in either a win or a draw against optimal play (never loses).
    - Test AI against various board states to ensure it chooses the highest-scoring move.
  </action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>All AI unit tests passing, confirming difficulty levels and unbeatable Hard mode.</done>
</task>

</tasks>

<verification>
Ensure CPU AI correctly calculates moves based on difficulty and never loses in Hard mode.
</verification>

<success_criteria>
- Hard AI never loses a match.
- Medium AI correctly blocks immediate threats.
- ./gradlew test passes all AI tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02/02-02-SUMMARY.md`
</output>
