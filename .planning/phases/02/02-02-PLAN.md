---
phase: 02
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt"
  - "app/src/test/java/com/jna/tictactoe/game/CpuPlayerTest.kt"
autonomous: true
requirements: ["2.3"]
must_haves:
  truths:
    - "Hard AI never loses (optimal play)"
    - "Medium AI blocks immediate human wins"
    - "Easy AI makes valid random moves"
    - "AI logic is pure Kotlin (no android.* imports)"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt"
      provides: "AI move calculation logic"
    - path: "app/src/test/java/com/jna/tictactoe/game/CpuPlayerTest.kt"
      provides: "AI competency and optimal play tests"
  key_links:
    - from: "app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt"
      to: "app/src/main/java/com/jna/tictactoe/game/GameEngine.kt"
      via: "simulation of moves for minimax"
---

<objective>
Implement three levels of CPU AI (Easy, Medium, Hard) using heuristic and minimax strategies.
Purpose: Provide varying levels of difficulty for the VS CPU game mode.
Output: Functioning AI that ranges from random moves to unbeatable optimal play, verified by unit tests.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02/02-RESEARCH.md
@.planning/phases/02/02-01-SUMMARY.md

<interfaces>
CpuPlayerTest.kt stub created in 02-00.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Easy and Medium CPU Strategies and Tests</name>
  <files>
    app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt,
    app/src/test/java/com/jna/tictactoe/game/CpuPlayerTest.kt
  </files>
  <behavior>
    - Easy: Pick random empty cell.
    - Medium: Block immediate human win, or take immediate CPU win.
  </behavior>
  <action>
    Implement CpuPlayer as a Kotlin object with getMove(state, difficulty).
    Fill in CpuPlayerTest.kt (from 02-00 stub) with tests for Easy (randomness/validity) and Medium (win/block logic).
    Strategies:
    - Easy: board.indices.filter { board[it] == EMPTY }.random()
    - Medium:
        1. Check if AI can win in one move -> Take it.
        2. Else check if Human can win in one move -> Block it.
        3. Else pick random empty cell.
  </action>
  <verify>
    <automated>./gradlew test --tests "*CpuPlayerTest*"</automated>
  </verify>
  <done>Easy and Medium strategies implemented and verified via unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Hard CPU Strategy (Minimax) and Tests</name>
  <files>
    app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt,
    app/src/test/java/com/jna/tictactoe/game/CpuPlayerTest.kt
  </files>
  <behavior>
    - Hard AI (Minimax) results in either a win or a draw against optimal play (never loses).
    - AI prefers immediate wins over later wins (depth adjustment).
  </behavior>
  <action>
    Implement Hard strategy using Minimax algorithm in CpuPlayer.kt.
    Update CpuPlayerTest.kt with scenarios where AI must force a draw or a win from complex states.
    Algorithm details:
    - Recursive minimax.
    - Scoring: Win (+10 - depth), Loss (depth - 10), Draw (0).
  </action>
  <verify>
    <automated>./gradlew test --tests "*CpuPlayerTest*"</automated>
  </verify>
  <done>Hard strategy implemented and verified as unbeatable.</done>
</task>

</tasks>

<verification>
Ensure CPU AI correctly calculates moves based on difficulty and never loses in Hard mode.
</verification>

<success_criteria>
- Hard AI never loses a match.
- Medium AI correctly blocks immediate threats.
- ./gradlew test passes all AI tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02/02-02-SUMMARY.md`
</output>
