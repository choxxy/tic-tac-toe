---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "app/src/main/java/com/jna/tictactoe/game/model/GameState.kt"
  - "app/src/main/java/com/jna/tictactoe/game/model/Player.kt"
  - "app/src/main/java/com/jna/tictactoe/game/model/CellState.kt"
  - "app/src/main/java/com/jna/tictactoe/game/model/GamePhase.kt"
  - "app/src/main/java/com/jna/tictactoe/game/model/GameMode.kt"
  - "app/src/main/java/com/jna/tictactoe/game/GameEngine.kt"
  - "app/src/test/java/com/jna/tictactoe/game/GameEngineTest.kt"
  - "app/src/test/java/com/jna/tictactoe/game/GameStateTest.kt"
autonomous: true
requirements: ["CORE-01", "CORE-02", "CORE-03", "CORE-04"]
must_haves:
  truths:
    - "GameEngine correctly identifies wins (8 lines)"
    - "GameEngine correctly identifies draws"
    - "GameEngine prevents moves on occupied cells"
    - "All logic is pure Kotlin (no android.* imports)"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/game/model/GameState.kt"
      provides: "Immutable game state representation"
    - path: "app/src/main/java/com/jna/tictactoe/game/GameEngine.kt"
      provides: "Core game rules and state transitions"
    - path: "app/src/test/java/com/jna/tictactoe/game/GameEngineTest.kt"
      provides: "Validation and rule enforcement tests"
  key_links:
    - from: "app/src/main/java/com/jna/tictactoe/game/GameEngine.kt"
      to: "app/src/main/java/com/jna/tictactoe/game/model/GameState.kt"
      via: "pure function state updates"
---

<objective>
Define pure Kotlin game models and implement the core Game Engine for rule enforcement and state management.
Purpose: Create a stable, testable, and UI-independent game foundation.
Output: Immutable game models, pure engine functions, and 100% test coverage on core rules.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Immutable Game Models</name>
  <files>
    app/src/main/java/com/jna/tictactoe/game/model/Player.kt,
    app/src/main/java/com/jna/tictactoe/game/model/CellState.kt,
    app/src/main/java/com/jna/tictactoe/game/model/GamePhase.kt,
    app/src/main/java/com/jna/tictactoe/game/model/GameMode.kt,
    app/src/main/java/com/jna/tictactoe/game/model/GameState.kt
  </files>
  <action>
    Create pure Kotlin models in com.jna.tictactoe.game.model.
    - Player: Enum (X, O, NONE).
    - CellState: Enum (X, O, EMPTY).
    - GamePhase: Enum (PLAYING, WIN, DRAW).
    - GameMode: Enum (VS_CPU, VS_HUMAN_LOCAL, VS_LAN).
    - GameState: Immutable data class holding board (List of CellState), currentTurn (Player), phase (GamePhase), mode (GameMode), and winLine (List of indices or null).
    - IMPORTANT: No android.* imports allowed.
  </action>
  <verify>
    <automated>./gradlew test -Ptest.filter=*GameStateTest*</automated>
  </verify>
  <done>All models defined and verified as immutable via GameStateTest.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GameEngine Core Logic</name>
  <files>app/src/main/java/com/jna/tictactoe/game/GameEngine.kt</files>
  <action>
    Implement GameEngine as a Kotlin object with pure functions:
    - isValidMove(state, index): Boolean. Returns true if board[index] is EMPTY and phase is PLAYING.
    - applyMove(state, index): GameState. Returns a new state with the move applied and turn toggled.
    - checkWin(board): List<Int>?. Returns the indices of the winning line if found, otherwise null. Must check 8 lines (3 rows, 3 cols, 2 diagonals).
    - checkDraw(board): Boolean. Returns true if board is full and no winner.
    - updateGameState(state): GameState. Internal helper to check win/draw and update phase.
    - IMPORTANT: No android.* imports.
  </action>
  <verify>
    <automated>./gradlew test -Ptest.filter=*GameEngineTest*</automated>
  </verify>
  <done>GameEngine logic implemented and verified against all win/draw scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Unit Tests for Models and Engine</name>
  <files>
    app/src/test/java/com/jna/tictactoe/game/GameEngineTest.kt,
    app/src/test/java/com/jna/tictactoe/game/GameStateTest.kt
  </files>
  <action>
    Create comprehensive unit tests using JUnit 4.
    - GameStateTest: Test initial state, and verify that applying a move results in a new instance (immutability).
    - GameEngineTest: Test move validation, all 8 winning lines (horizontal, vertical, diagonal), and draw detection.
    - Test edge cases: Moving twice on same cell, moving after game end.
  </action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>All unit tests passing with 100% coverage on rule detection logic.</done>
</task>

</tasks>

<verification>
Verify that the core game logic is pure Kotlin and passes all unit tests.
</verification>

<success_criteria>
- GameEngine and Models have zero android.* imports.
- ./gradlew test passes all unit tests.
- All 8 win lines are correctly detected by GameEngine.
</success_criteria>

<output>
After completion, create `.planning/phases/02/02-01-SUMMARY.md`
</output>
