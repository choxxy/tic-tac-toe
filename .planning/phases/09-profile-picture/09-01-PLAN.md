---
phase: 09-profile-picture
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - app/src/main/AndroidManifest.xml
autonomous: true
requirements:
  - PROFILE-01
must_haves:
  artifacts:
    - path: "app/build.gradle.kts"
      contains: "libs.accompanist.permissions"
    - path: "app/build.gradle.kts"
      contains: "libs.coil.compose"
    - path: "app/src/main/AndroidManifest.xml"
      contains: "<uses-permission android:name="android.permission.CAMERA" />"
---

<objective>
This plan adds the necessary dependencies for image handling and declares the required permissions for camera access.
</objective>

<context>
@app/build.gradle.kts
@app/src/main/AndroidManifest.xml
@gradle/libs.versions.toml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Dependencies</name>
  <files>
    - gradle/libs.versions.toml
    - app/build.gradle.kts
  </files>
  <action>
    1.  Add `coil-compose` for image loading and `accompanist-permissions` for runtime permission handling to `gradle/libs.versions.toml`. Use the latest stable versions.
        - `coil-compose` = "io.coil-kt:coil-compose:..."
        - `accompanist-permissions` = "com.google.accompanist:accompanist-permissions:..."
    2.  Add the new library aliases to the `dependencies` block in `app/build.gradle.kts`.
  </action>
  <verify>
    The project builds successfully after adding the dependencies.
  </verify>
  <done>
    `coil-compose` and `accompanist-permissions` are added to the project dependencies.
  </done>
</task>
<task type="auto">
  <name>Task 2: Declare Camera Permission</name>
  <files>
    - app/src/main/AndroidManifest.xml
  </files>
  <action>
    Add the `<uses-permission android:name="android.permission.CAMERA" />` and `<uses-feature android:name="android.hardware.camera" android:required="false" />` to the `app/src/main/AndroidManifest.xml` file. The feature is not required as users can still pick from the gallery.
  </action>
  <verify>
    The `AndroidManifest.xml` contains the `CAMERA` permission and feature declarations.
  </verify>
  <done>
    Camera permission is declared in the manifest.
  </done>
</task>
</tasks>

<success_criteria>
The project is configured with the necessary dependencies and permissions for the profile picture feature.
</success_criteria>
