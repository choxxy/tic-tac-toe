---
phase: 06-dependency-injection
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt, app/src/main/java/com/jna/tictactoe/screen/lobby/LanLobbyViewModel.kt, app/src/main/java/com/jna/tictactoe/MainActivity.kt, app/src/main/java/com/jna/tictactoe/audio/AudioProvider.kt, app/src/main/java/com/jna/tictactoe/network/socket/SocketProvider.kt, app/src/main/java/com/jna/tictactoe/screen/lobby/SocketProvider.kt]
autonomous: true
requirements: [6.2]

must_haves:
  truths:
    - "ViewModels are successfully injected with Hilt singletons"
    - "Session scores persist when navigating between Game and Menu"
    - "Manual Provider classes are removed from the codebase"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt"
      provides: "Hilt-enabled GameViewModel"
      contains: "@HiltViewModel"
    - path: "app/src/main/java/com/jna/tictactoe/MainActivity.kt"
      provides: "Hilt-enabled MainActivity"
      contains: "@AndroidEntryPoint"
  key_links:
    - from: "app/src/main/java/com/jna/tictactoe/MainActivity.kt"
      to: "GameViewModel"
      via: "hiltViewModel"
      pattern: "hiltViewModel"
---

<objective>
Refactor ViewModels to use Hilt dependency injection, update MainActivity and NavHost to use Hilt's ViewModel factory, and clean up the legacy manual provider classes.

Purpose: Complete the DI refactor and remove "singleton-as-object" antipatterns.
Output: Modernized ViewModels and cleaner architecture.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt
@app/src/main/java/com/jna/tictactoe/screen/lobby/LanLobbyViewModel.kt
@app/src/main/java/com/jna/tictactoe/MainActivity.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ViewModels with Hilt</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt, app/src/main/java/com/jna/tictactoe/screen/lobby/LanLobbyViewModel.kt</files>
  <action>
    - For `GameViewModel`:
        - Annotate with `@HiltViewModel`.
        - Add `@Inject constructor` with `savedStateHandle: SavedStateHandle`, `socketManager: GameSocketManager`, and `soundManager: SoundManager`.
        - Switch from `AndroidViewModel(application)` to `ViewModel()`.
        - Remove manual provider calls (`SocketProvider.gameSocketManager`, `AudioProvider.getInstance`).
    - For `LanLobbyViewModel`:
        - Annotate with `@HiltViewModel`.
        - Add `@Inject constructor` with `gameSocketManager: GameSocketManager` and `nsdDiscoveryManager: NsdDiscoveryManager`.
        - Switch from `AndroidViewModel(application)` to `ViewModel()`.
        - Remove manual provider calls and manual `NsdDiscoveryManager` instantiation.
  </action>
  <verify>
    <automated>./gradlew app:assembleDebug</automated>
  </verify>
  <done>ViewModels are refactored to use constructor injection via Hilt.</done>
</task>

<task type="auto">
  <name>Task 2: Update MainActivity and Navigation</name>
  <files>app/src/main/java/com/jna/tictactoe/MainActivity.kt</files>
  <action>
    - Annotate `MainActivity` with `@AndroidEntryPoint`.
    - In `MainActivity.kt`, update `gameViewModel` instantiation to use Hilt's scoping: `val gameViewModel: GameViewModel = hiltViewModel(viewModelStoreOwner = LocalContext.current as ComponentActivity)`. This ensures session-long score persistence as it scopes the ViewModel to the Activity.
    - In the `Lobby` composable, replace `val lobbyViewModel: LanLobbyViewModel = viewModel()` with `val lobbyViewModel: LanLobbyViewModel = hiltViewModel()`.
  </action>
  <verify>
    <automated>./gradlew app:assembleDebug</automated>
  </verify>
  <done>Activity and NavHost are updated to leverage Hilt's ViewModel factories.</done>
</task>

<task type="auto">
  <name>Task 3: Cleanup and Final Verification</name>
  <files>app/src/main/java/com/jna/tictactoe/audio/AudioProvider.kt, app/src/main/java/com/jna/tictactoe/network/socket/SocketProvider.kt, app/src/main/java/com/jna/tictactoe/screen/lobby/SocketProvider.kt</files>
  <action>
    - Delete `AudioProvider.kt`, `SocketProvider.kt` (network/socket), and `SocketProvider.kt` (screen/lobby).
    - Run `GameViewModelAudioTest.kt` to ensure it still passes (requires updating test setup to manually inject mocks into the constructor).
    - Perform final build and verify app functionality.
  </action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>Legacy providers removed and project verified.</done>
</task>

</tasks>

<verification>
All ViewModels are injected via Hilt, and the app functions correctly across all game modes.
</verification>

<success_criteria>
- App runs without crashes on startup or navigation.
- LAN multiplayer discovery and connectivity works as before.
- Audio feedback still functions correctly.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06/06-02-SUMMARY.md`
</output>
