---
phase: 06-dependency-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [gradle/libs.versions.toml, app/build.gradle.kts, build.gradle.kts, app/src/main/AndroidManifest.xml, app/src/main/java/com/jna/tictactoe/TicTacToeApplication.kt, app/src/main/java/com/jna/tictactoe/di/AppModule.kt]
autonomous: true
requirements: [6.1]

must_haves:
  truths:
    - "Project builds and app launches with Hilt initialized"
    - "SoundManager and GameSocketManager are available as Singletons"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/TicTacToeApplication.kt"
      provides: "Hilt application entry point"
      contains: "@HiltAndroidApp"
    - path: "app/src/main/java/com/jna/tictactoe/di/AppModule.kt"
      provides: "Hilt dependency module"
      exports: ["SoundManager", "GameSocketManager", "NsdDiscoveryManager"]
  key_links:
    - from: "app/build.gradle.kts"
      to: "hilt"
      via: "plugins and dependencies"
      pattern: "id\\(\"com.google.dagger.hilt.android\"\\)"
---

<objective>
Setup Hilt infrastructure, including dependencies, Application class, and the base AppModule to provide core singleton dependencies (SoundManager, GameSocketManager, NsdDiscoveryManager).

Purpose: Establish a robust dependency injection foundation to improve code maintainability and testability.
Output: Hilt-enabled project with centralized singleton management.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/com/jna/tictactoe/audio/SoundManager.kt
@app/src/main/java/com/jna/tictactoe/network/socket/GameSocketManager.kt
@app/src/main/java/com/jna/tictactoe/network/discovery/NsdDiscoveryManager.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Hilt Dependencies</name>
  <files>gradle/libs.versions.toml, app/build.gradle.kts, build.gradle.kts</files>
  <action>
    - Add `kapt` plugin to `app/build.gradle.kts`: `id("org.jetbrains.kotlin.kapt")`.
    - Apply `alias(libs.plugins.hilt)` in `app/build.gradle.kts` and ensure it's in `build.gradle.kts` (project level).
    - Add the following dependencies to `app/build.gradle.kts`:
        - `implementation(libs.hilt.android)`
        - `kapt(libs.hilt.compiler)`
        - `implementation(libs.androidx.hilt.navigation.compose)`
  </action>
  <verify>
    <automated>./gradlew app:assembleDebug</automated>
  </verify>
  <done>Hilt plugins and dependencies successfully added to build configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Create TicTacToeApplication</name>
  <files>app/src/main/java/com/jna/tictactoe/TicTacToeApplication.kt, app/src/main/AndroidManifest.xml</files>
  <action>
    - Create `TicTacToeApplication.kt` in `com.jna.tictactoe` package.
    - Annotate it with `@HiltAndroidApp`.
    - Inherit from `android.app.Application`.
    - Update `AndroidManifest.xml` to include `android:name=".TicTacToeApplication"` in the `<application>` tag.
  </action>
  <verify>
    <automated>./gradlew app:assembleDebug</automated>
  </verify>
  <done>Application class created and registered in manifest.</done>
</task>

<task type="auto">
  <name>Task 3: Implement AppModule</name>
  <files>app/src/main/java/com/jna/tictactoe/di/AppModule.kt</files>
  <action>
    - Create `AppModule.kt` in `com.jna.tictactoe.di` package.
    - Annotate with `@Module` and `@InstallIn(SingletonComponent::class)`.
    - Provide `@Singleton` instances of:
        - `SoundManager(context: Context)` (use `@ApplicationContext`).
        - `GameSocketManager()` (no arguments).
        - `NsdDiscoveryManager(context: Context)` (use `@ApplicationContext`).
  </action>
  <verify>
    <automated>./gradlew app:assembleDebug</automated>
  </verify>
  <done>Core dependencies are centrally provided via Hilt AppModule.</done>
</task>

</tasks>

<verification>
Hilt dependencies are successfully resolved and the project builds.
</verification>

<success_criteria>
- Project compiles without Hilt annotation errors.
- AppModule correctly provides required singletons.
</success_criteria>

<output>
After completion, create `.planning/phases/06/06-01-SUMMARY.md`
</output>
