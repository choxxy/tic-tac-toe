---
phase: 01-foundation-navigation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [app/src/main/java/com/jna/tictactoe/screen/splash/SplashScreen.kt, app/src/main/java/com/jna/tictactoe/SplashScreen.kt, app/src/main/java/com/jna/tictactoe/navigation/Routes.kt, app/src/main/java/com/jna/tictactoe/MainActivity.kt]
autonomous: true
requirements: [INFRA-01, NAV-01]

must_haves:
  truths:
    - "SplashScreen is correctly located in screen/splash/ package"
    - "Type-safe route contracts are defined and @Serializable"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/screen/splash/SplashScreen.kt"
      provides: "Relocated Splash component"
    - path: "app/src/main/java/com/jna/tictactoe/navigation/Routes.kt"
      provides: "Type-safe route objects"
---

<objective>
Refactor the existing codebase into the new architectural structure and define the type-safe navigation contracts.

Purpose: Clear the root package and prepare for structured navigation using Kotlin Serialization.
Output: Relocated source files and a centralized routes contract file.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01/01-RESEARCH.md
@app/src/main/java/com/jna/tictactoe/SplashScreen.kt
@app/src/main/java/com/jna/tictactoe/MainActivity.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate legacy files</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/splash/SplashScreen.kt, app/src/main/java/com/jna/tictactoe/SplashScreen.kt, app/src/main/java/com/jna/tictactoe/MainActivity.kt</files>
  <action>
    1. Move `app/src/main/java/com/jna/tictactoe/SplashScreen.kt` to `app/src/main/java/com/jna/tictactoe/screen/splash/SplashScreen.kt`.
    2. Update package declaration in `SplashScreen.kt` to `package com.jna.tictactoe.screen.splash`.
    3. Update `MainActivity.kt` imports for `SplashScreen`.
    4. Run `./gradlew help` to verify compilation.
  </action>
  <verify>
    <automated>./gradlew help</automated>
  </verify>
  <done>Legacy files are relocated and app still compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Define type-safe route contracts</name>
  <files>app/src/main/java/com/jna/tictactoe/navigation/Routes.kt</files>
  <action>
    Create `app/src/main/java/com/jna/tictactoe/navigation/Routes.kt` with the following:
    - Package: `com.jna.tictactoe.navigation`
    - Imports: `kotlinx.serialization.Serializable`
    - Content:
        - `@Serializable object Splash`
        - `@Serializable object Menu`
        - `@Serializable object Lobby`
        - `@Serializable object Game`
  </action>
  <verify>
    <automated>grep "@Serializable object" app/src/main/java/com/jna/tictactoe/navigation/Routes.kt</automated>
  </verify>
  <done>Navigation route contracts are defined and serializable.</done>
</task>

</tasks>

<verification>
Check if project compiles after refactor.
Check if Routes.kt exists with correct package and objects.
</verification>

<success_criteria>
- Project compiles without errors.
- Routes are defined as `@Serializable` objects.
</success_criteria>

<output>
After completion, create `.planning/phases/01/01-02-SUMMARY.md`
</output>
