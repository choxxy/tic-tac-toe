---
phase: 01-foundation-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [gradle/libs.versions.toml, app/build.gradle.kts, build.gradle.kts]
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "Project syncs successfully with new dependencies"
    - "Kotlin serialization plugin is active"
    - "New directory structure exists on disk"
  artifacts:
    - path: "gradle/libs.versions.toml"
      provides: "Version definitions for navigation, serialization, and lifecycle"
    - path: "app/build.gradle.kts"
      provides: "Applied plugins and dependencies"
  key_links:
    - from: "app/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "libs.plugins and libs.androidx references"
---

<objective>
Establish the project's foundational infrastructure by configuring dependencies and the architectural package structure.

Purpose: Enable type-safe navigation and modern lifecycle management using the latest stable libraries.
Output: Updated build configuration and directory scaffolding.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01/01-RESEARCH.md
@gradle/libs.versions.toml
@app/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define versions and libraries</name>
  <files>gradle/libs.versions.toml</files>
  <action>
    Add the following to `gradle/libs.versions.toml`:
    - `navigationCompose = "2.8.8"` (per user constraint)
    - `kotlinxSerialization = "1.9.0"` (per Research)
    - Ensure `lifecycleRuntimeKtx = "2.10.0"` (exists, but verify)
    - Add to `[libraries]`:
        - `androidx-navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigationCompose" }`
        - `kotlinx-serialization-json = { group = "org.jetbrains.kotlinx", name = "kotlinx-serialization-json", version.ref = "kotlinxSerialization" }`
        - `androidx-lifecycle-runtime-compose = { group = "androidx.lifecycle", name = "lifecycle-runtime-compose", version.ref = "lifecycleRuntimeKtx" }`
        - `androidx-lifecycle-viewmodel-compose = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-compose", version.ref = "lifecycleRuntimeKtx" }`
    - Add to `[plugins]`:
        - `kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }`
  </action>
  <verify>
    <automated>grep -E "navigationCompose|kotlinxSerialization" gradle/libs.versions.toml</automated>
  </verify>
  <done>Versions and libraries are defined in the TOML file.</done>
</task>

<task type="auto">
  <name>Task 2: Configure build plugins and dependencies</name>
  <files>build.gradle.kts, app/build.gradle.kts</files>
  <action>
    1. In root `build.gradle.kts`, add `alias(libs.plugins.kotlin.serialization) apply false` to the `plugins` block.
    2. In `app/build.gradle.kts`:
        - Add `alias(libs.plugins.kotlin.serialization)` to the `plugins` block.
        - Add the following to `dependencies`:
            - `implementation(libs.androidx.navigation.compose)`
            - `implementation(libs.kotlinx.serialization.json)`
            - `implementation(libs.androidx.lifecycle.runtime.compose)`
            - `implementation(libs.androidx.lifecycle.viewmodel.compose)`
    3. Run `./gradlew help` to trigger a sync.
  </action>
  <verify>
    <automated>./gradlew help</automated>
  </verify>
  <done>Project syncs successfully with new plugins and dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Establish package structure</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/.gitkeep, app/src/main/java/com/jna/tictactoe/navigation/.gitkeep, app/src/main/java/com/jna/tictactoe/game/.gitkeep, app/src/main/java/com/jna/tictactoe/network/.gitkeep</files>
  <action>
    Create the following directories under `app/src/main/java/com/jna/tictactoe/`:
    - `screen/splash/`
    - `screen/menu/`
    - `navigation/`
    - `game/`
    - `network/`
    Add `.gitkeep` files to ensure they are tracked.
  </action>
  <verify>
    <automated>ls -R app/src/main/java/com/jna/tictactoe/</automated>
  </verify>
  <done>Architectural package structure is established.</done>
</task>

</tasks>

<verification>
Check if `./gradlew help` passes.
Check if all directories exist.
</verification>

<success_criteria>
- Project syncs without errors.
- New packages are visible in the file system.
</success_criteria>

<output>
After completion, create `.planning/phases/01/01-01-SUMMARY.md`
</output>
