---
phase: 01-foundation-navigation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [app/src/main/java/com/jna/tictactoe/screen/menu/MainMenuScreen.kt, app/src/main/java/com/jna/tictactoe/MainActivity.kt, app/src/main/java/com/jna/tictactoe/screen/splash/SplashScreen.kt, app/src/androidTest/java/com/jna/tictactoe/NavigationTest.kt]
autonomous: true
requirements: [NAV-02, MENU-01, SPLASH-01]

must_haves:
  truths:
    - "MainActivity hosts the NavHost with typed routes"
    - "MainMenuScreen is rendered on the menu route"
    - "SplashScreen auto-advances to Menu via NavHost callback"
    - "Backstack is cleared: Menu exits app instead of returning to Splash"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/screen/menu/MainMenuScreen.kt"
      provides: "Menu screen UI with navigation buttons"
    - path: "app/src/main/java/com/jna/tictactoe/MainActivity.kt"
      provides: "Central NavHost implementation with popUpTo logic"
  key_links:
    - from: "app/src/main/java/com/jna/tictactoe/MainActivity.kt"
      to: "app/src/main/java/com/jna/tictactoe/navigation/Routes.kt"
      via: "NavHost destinations"
---

<objective>
Implement the core navigation flow from Splash to Menu using Jetpack Navigation with type-safe routes.

Purpose: Activate the defined navigation architecture and provide the primary user entry point.
Output: Working NavHost, placeholder Main Menu, and automated Splash advance.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01/01-RESEARCH.md
@app/src/main/java/com/jna/tictactoe/MainActivity.kt
@app/src/main/java/com/jna/tictactoe/screen/splash/SplashScreen.kt
@app/src/main/java/com/jna/tictactoe/navigation/Routes.kt
@app/src/androidTest/java/com/jna/tictactoe/NavigationTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement placeholder Main Menu</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/menu/MainMenuScreen.kt</files>
  <action>
    Create `app/src/main/java/com/jna/tictactoe/screen/menu/MainMenuScreen.kt`:
    - Package: `com.jna.tictactoe.screen.menu`
    - Component: `MainMenuScreen(onVsCpu: () -> Unit, onVsLocal: () -> Unit, onVsLan: () -> Unit)`
    - Content: Vertical column with "Zenith Grid" title and buttons for each mode (placeholders).
    - Styling: Use `MaterialTheme.typography.headlineLarge` for the title.
  </action>
  <verify>
    <automated>grep "MainMenuScreen" app/src/main/java/com/jna/tictactoe/screen/menu/MainMenuScreen.kt</automated>
  </verify>
  <done>MainMenuScreen is implemented with navigation callbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Configure NavHost in MainActivity</name>
  <files>app/src/main/java/com/jna/tictactoe/MainActivity.kt</files>
  <action>
    Modify `MainActivity.kt`:
    1. Initialize `rememberNavController()`.
    2. Replace the direct `SplashScreen()` call in `setContent` with a `NavHost`.
    3. Define destinations for `Splash` and `Menu` using `composable<Splash>` and `composable<Menu>`.
    4. Pass navigation callbacks to screens.
    5. CRITICAL: The `onTimeout` for `Splash` destination MUST implement navigation to `Menu` with `popUpTo(Splash) { inclusive = true }` to clear the backstack.
  </action>
  <verify>
    <automated>grep -E "NavHost|popUpTo\(Splash\)" app/src/main/java/com/jna/tictactoe/MainActivity.kt</automated>
  </verify>
  <done>MainActivity uses NavHost with correct backstack management.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Splash auto-advance logic and test</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/splash/SplashScreen.kt, app/src/androidTest/java/com/jna/tictactoe/NavigationTest.kt</files>
  <behavior>
    - The Splash screen should show "Zenith Grid" (or splash content).
    - After 2 seconds, it should trigger the onTimeout callback.
    - Navigation test should verify that the app transitions from Splash to Menu automatically.
  </behavior>
  <action>
    1. Update `SplashScreen.kt`:
        - Add `onTimeout: () -> Unit` parameter.
        - Implement `LaunchedEffect(Unit) { delay(2000); onTimeout() }`.
    2. Update `NavigationTest.kt` to include a test case that:
        - Starts the activity.
        - Checks for splash content.
        - Waits for transition.
        - Checks for menu content (e.g., "Zenith Grid" title or buttons).
  </action>
  <verify>
    <automated>./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.jna.tictactoe.NavigationTest</automated>
  </verify>
  <done>SplashScreen auto-advances and behavior is verified by UI test.</done>
</task>

</tasks>

<verification>
Check if project compiles and automated test passes.
</verification>

<success_criteria>
- App compiles and runs.
- NavHost correctly handles Splash and Menu destinations.
- Backstack is cleared (popUpTo Splash).
- NavigationTest verifies the auto-advance flow.
</success_criteria>

<output>
After completion, create `.planning/phases/01/01-03-SUMMARY.md`
</output>
