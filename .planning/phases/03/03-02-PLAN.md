---
phase: 03-game-ui
plan: 02
type: execute
wave: 3
depends_on: ["03-game-ui-01"]
files_modified: [
  "app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt",
  "app/src/main/java/com/jna/tictactoe/MainActivity.kt",
  "app/src/main/java/com/jna/tictactoe/screen/menu/MainMenuScreen.kt",
  "app/src/androidTest/java/com/jna/tictactoe/screen/game/GameScreenTest.kt"
]
autonomous: false
requirements: ["3.2", "3.3"]
must_haves:
  truths:
    - "GameBoard has no visible lines (uses surface tiers)"
    - "ResultDialog appears on Win/Draw"
    - "Play Again resets board, New Match resets score and returns home"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt"
      provides: "UI implementation"
    - path: "app/src/main/java/com/jna/tictactoe/MainActivity.kt"
      provides: "NavHost integration"
---

<objective>
Implement the Game UI using Jetpack Compose, adhering to the 'Architectural Serenity' design system. Wire the screen to the GameViewModel, implement the ResultDialog, and connect navigation from the MainMenuScreen.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt
@app/src/main/java/com/jna/tictactoe/ui/theme/Color.kt
@designs/zenith_grid/DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement GameBoard and Components</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/game/GameScreen.kt</files>
  <action>
    Implement the `GameScreen` and its sub-components:
    - `GameBoard`: 3x3 grid with `Arrangement.spacedBy` and tonal depth background (`surfaceContainerHigh`).
    - `GameCell`: Individual cell with `surfaceContainerLowest` background, rounded corners, and X/O icons.
    - `ScoreBoard`: Header displaying Player X and Player O wins.
    - `ResultDialog`: Material 3 Dialog for game outcomes.
    
    Ensure strict UDF: Screen observes `viewModel.uiState` and emits events.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Game UI is built and connected to ViewModel state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Navigation and MainMenu interactions</name>
  <files>
    app/src/main/java/com/jna/tictactoe/MainActivity.kt,
    app/src/main/java/com/jna/tictactoe/screen/menu/MainMenuScreen.kt
  </files>
  <action>
    1. Update `MainActivity.kt`: Add `composable<Game>` route to `NavHost`. Provide a factory to `viewModel()` or use Hilt (if available, but requirements say none added). Scoping to `composable` level is enough.
    2. Update `MainMenuScreen.kt`: Implement `onVsCpu` (navigates to `Game(mode=VS_CPU_EASY, ...)` - Easy for now) and `onVsLocal` (navigates to `Game(mode=VS_HUMAN_LOCAL)`).
    3. Update `GameScreen.kt`: Implement "Quit" button and "New Match" button to navigate back to `Menu`.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>App navigation flow is complete and functional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Functional Game UI with Local and CPU play modes.</what-built>
  <how-to-verify>
    1. Run app on emulator/device.
    2. Navigate to Game from Main Menu.
    3. Verify "Local" mode: Play a full game, check if turn indicators and board updates work.
    4. Verify "VS CPU" mode: Play a game, check if CPU moves automatically after a short delay.
    5. Verify Win/Draw ResultDialog and "Play Again" / "New Match" functionality.
    6. Verify "Architectural Serenity" UI (no lines, surface tiers).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Implement Automated UI Tests</name>
  <files>app/src/androidTest/java/com/jna/tictactoe/screen/game/GameScreenTest.kt</files>
  <action>
    Update the test skeleton to verify:
    - Board cells are clickable.
    - Win outcome triggers Dialog.
    - Reset preserves scores.
  </action>
  <verify>
    <automated>./gradlew connectedCheck</automated>
  </verify>
  <done>UI tests pass on device/emulator.</done>
</task>

</tasks>

<verification>
Perform a full playthrough of all game modes and run all automated tests.
</verification>

<success_criteria>
- Game board is visually beautiful and follows design rules.
- Local gameplay works seamlessly.
- CPU opponent behaves naturally with appropriate delays.
- Session score is accurate and resets appropriately.
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-ui/03-02-SUMMARY.md`
</output>
