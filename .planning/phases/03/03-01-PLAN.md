---
phase: 03-game-ui
plan: 01
type: tdd
wave: 2
depends_on: ["03-game-ui-00"]
files_modified: [
  "app/src/main/java/com/jna/tictactoe/screen/game/GameUiState.kt",
  "app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt",
  "app/src/test/java/com/jna/tictactoe/screen/game/GameViewModelTest.kt"
]
autonomous: true
requirements: ["3.1"]
must_haves:
  truths:
    - "ViewModel tracks game state changes from engine"
    - "AI moves occur after a 600-800ms delay"
    - "Session score increases on win but doesn't reset on game reset"
  artifacts:
    - path: "app/src/main/java/com/jna/tictactoe/screen/game/GameUiState.kt"
      provides: "UI State model"
    - path: "app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt"
      provides: "UDF Logic"
  key_links:
    - from: "app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt"
      to: "app/src/main/java/com/jna/tictactoe/game/GameEngine.kt"
      via: "Move validation and application"
    - from: "app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt"
      to: "app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt"
      via: "AI move retrieval"
---

<objective>
Implement the core game logic in the GameViewModel using TDD. This includes state management via StateFlow, integration with the GameEngine and CpuPlayer, implementing AI turn delays, and tracking session scores.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/com/jna/tictactoe/game/GameEngine.kt
@app/src/main/java/com/jna/tictactoe/game/CpuPlayer.kt
@app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt
</context>

<feature>
  <name>Game View Model Logic</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt, app/src/test/java/com/jna/tictactoe/screen/game/GameViewModelTest.kt</files>
  <behavior>
    - Initialization: Takes GameMode and optional Difficulty, sets initial GameState.
    - Player Move: Validates move via GameEngine, updates board, checks for win/draw.
    - AI Move: In VS_CPU mode, triggers AI move after a delay when it's the CPU's turn.
    - Session Score: Tracks wins for X, wins for O, and Draws.
    - Reset: Resets GameEngine state for a new game but preserves the session score.
  </behavior>
  <implementation>
    - Use `MutableStateFlow<GameUiState>` to expose state.
    - Use `viewModelScope` and `delay()` for AI thinking time.
    - Store `Score` as a separate data class or within `GameUiState`.
    - Integration: Use `GameEngine.applyMove` and `CpuPlayer.getMove`.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Define GameUiState and Score models</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/game/GameUiState.kt</files>
  <action>
    Create `GameUiState` data class holding:
    - `gameState: GameState`
    - `xWins: Int`
    - `oWins: Int`
    - `draws: Int`
    - `isThinking: Boolean` (for AI delay period)
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>UI State model is defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GameViewModel Logic (TDD)</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt, app/src/test/java/com/jna/tictactoe/screen/game/GameViewModelTest.kt</files>
  <behavior>
    - Test: On initialization, state matches provided mode/difficulty.
    - Test: On cell click, state updates via engine.
    - Test: In VS_CPU, after player move, AI move is triggered (simulate with advanceTime if using TestDispatcher).
    - Test: On win, score is incremented.
    - Test: On resetGame, board is cleared but score is preserved.
  </behavior>
  <action>
    Implement the ViewModel logic to pass all behavior tests. Use `SavedStateHandle` to retrieve navigation arguments if possible, or a manual `init` call from the factory. (Preference: Retrieve from `SavedStateHandle` as `androidx.navigation.toRoute<Game>()` is supported in late 2.8.x).
  </action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>GameViewModel logic is fully implemented and verified by tests.</done>
</task>

</tasks>

<verification>
Run all unit tests to ensure game logic, AI timing (simulated), and score persistence are correct.
</verification>

<success_criteria>
- GameViewModel correctly manages the flow of a Tic-Tac-Toe match.
- VS CPU mode handles AI turns autonomously with delays.
- Session scores are correctly tracked across multiple games in the same ViewModel instance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-ui/03-01-SUMMARY.md`
</output>
