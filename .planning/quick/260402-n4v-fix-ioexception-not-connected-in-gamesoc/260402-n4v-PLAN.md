---
phase: quick
plan: 260402-n4v
type: execute
wave: 1
depends_on: []
files_modified: [
  "app/src/main/java/com/jna/tictactoe/network/socket/GameSocketManager.kt",
  "app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt",
  "app/src/main/java/com/jna/tictactoe/screen/lobby/LanLobbyViewModel.kt",
  "app/src/test/java/com/jna/tictactoe/network/socket/GameSocketManagerTest.kt"
]
autonomous: true
requirements: [LAN-04]
---

<objective>
Fix IOException: "Not connected" in GameSocketManager.kt:131.

Purpose: Prevent app crashes and log noise when sending messages while the socket is not connected or recently disconnected.
Output: Resilient socket communication with graceful state handling.
</objective>

<execution_context>
@/Users/choxxy/Projects/tictactoe/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/com/jna/tictactoe/network/socket/GameSocketManager.kt
@app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor GameSocketManager.send to return Boolean</name>
  <files>app/src/main/java/com/jna/tictactoe/network/socket/GameSocketManager.kt</files>
  <action>
    Modify `GameSocketManager.kt`:
    1. Add `val isConnected: Boolean get() = writer != null`.
    2. Change `send(message: GameMessage)` return type from `Unit` to `Boolean`.
    3. Update implementation: 
       - Return `false` if `writer` is null (do NOT throw `IOException`).
       - Wrap `println` in a try-catch, return `true` on success.
       - Return `false` on any exception and log it (unless it's a standard disconnect).
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "com.jna.tictactoe.network.socket.GameSocketManagerTest"</automated>
  </verify>
  <done>GameSocketManager.send returns Boolean and does not throw for "Not connected" state.</done>
</task>

<task type="auto">
  <name>Task 2: Update ViewModels to handle send() results</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/game/GameViewModel.kt, app/src/main/java/com/jna/tictactoe/screen/lobby/LanLobbyViewModel.kt</files>
  <action>
    1. In `GameViewModel.kt`:
       - Update `startHeartbeat()`: replace `try-catch` with `if (!socketManager.send(GameMessage.Heartbeat)) { handleDisconnect(); break }`.
       - Update `onCellClick()`: wrap `socketManager.send(GameMessage.Move(index))` in `if (!socketManager.send(...)) { handleDisconnect() }`.
       - Update `broadcastState()`: wrap `socketManager.send(...)` in `if (!socketManager.send(...)) { handleDisconnect() }`.
    2. In `LanLobbyViewModel.kt`:
       - Update `performHandshake()` to return `Boolean`.
       - Update call site in `viewModelScope.launch` to check the result if necessary, though it is already inside a `try-catch` for `connect()`.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <done>ViewModels handle failed send operations without crashing.</done>
</task>

<task type="auto">
  <name>Task 3: Update and expand unit tests</name>
  <files>app/src/test/java/com/jna/tictactoe/network/socket/GameSocketManagerTest.kt</files>
  <action>
    1. Update existing `testBidirectionalCommunication` to ignore/match new return values.
    2. Add `testSendWhenNotConnectedReturnsFalse()`:
       - Instantiate `GameSocketManager` without calling `host` or `connect`.
       - Assert `manager.send(GameMessage.Heartbeat)` returns `false`.
       - Assert no exception is thrown.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "com.jna.tictactoe.network.socket.GameSocketManagerTest"</automated>
  </verify>
  <done>Unit tests verify that sending while disconnected returns false instead of throwing.</done>
</task>

</tasks>

<success_criteria>
1. `GameSocketManager.send()` returns `false` instead of throwing `IOException` when `writer` is null.
2. App does not crash when attempting to move or broadcast state while the connection is unstable.
3. Unit tests pass for both success and disconnected failure cases.
</success_criteria>

<output>
After completion, create `.planning/quick/260402-n4v-fix-ioexception-not-connected-in-gamesoc/260402-n4v-SUMMARY.md`
</output>
