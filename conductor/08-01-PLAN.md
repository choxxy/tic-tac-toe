---
phase: 08-profile-settings
plan: 01
type: execute
wave: 1
---

<objective>
Implement the Player Profile and Settings screens, using Jetpack DataStore for persistent storage, and replace hardcoded data in the Main Menu.

NOTE: This plan should actually be executed and stored in `.planning/phases/08/` per the GSD workflow, not the conductor folder.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement DataStore and Models</name>
  <files>gradle/libs.versions.toml, app/build.gradle.kts, app/src/main/java/com/jna/tictactoe/data/UserPreferences.kt, app/src/main/java/com/jna/tictactoe/data/PreferenceRepository.kt, app/src/main/java/com/jna/tictactoe/di/AppModule.kt</files>
  <action>
    - Add `androidx.datastore:datastore-preferences` to dependencies.
    - Create `UserPreferences` data class (name, rank, winRate, soundEnabled, hapticsEnabled, darkModeEnabled).
    - Create `PreferenceRepository` using `DataStore<Preferences>`.
    - Update `AppModule` to provide `PreferenceRepository` as a singleton.
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Profile and Settings Screens</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/settings/SettingsScreen.kt, app/src/main/java/com/jna/tictactoe/screen/settings/SettingsViewModel.kt, app/src/main/java/com/jna/tictactoe/screen/profile/ProfileScreen.kt, app/src/main/java/com/jna/tictactoe/screen/profile/ProfileViewModel.kt</files>
  <action>
    - Create `SettingsScreen` and `SettingsViewModel` with toggles for preferences.
    - Create `ProfileScreen` and `ProfileViewModel` for editing user name and avatar.
    - Follow Zenith Grid design guidelines.
  </action>
</task>

<task type="auto">
  <name>Task 3: Update Main Menu and Navigation</name>
  <files>app/src/main/java/com/jna/tictactoe/screen/menu/MainMenuScreen.kt, app/src/main/java/com/jna/tictactoe/MainActivity.kt, app/src/main/java/com/jna/tictactoe/navigation/Routes.kt</files>
  <action>
    - Update `Routes.kt` with `Settings` and `Profile` routes.
    - Update `MainActivity.kt` to include new screens in `NavHost`.
    - Update `MainMenuScreen.kt` to observe `UserPreferences` and use real data instead of hardcoded values.
  </action>
</task>

</tasks>